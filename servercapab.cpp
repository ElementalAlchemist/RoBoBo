#include "servercapab.h"

std::vector<char> statusModes[2];
std::vector<char> listModes, allParamModes, setParamModes, paramlessModes, chanTypes;

void handleCapab(std::vector<std::string> parsedLine) {
	// handle 005 input when I get that far
}