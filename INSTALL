This bot can be compiled using make, at which point the binary can be run
directly from the base directory.
Currently if you wish to load any modules, you must manually add them to the
Makefile.  This will be changed in the future.